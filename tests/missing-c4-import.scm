(require-extension scheme extras)

;; Syntax error (import): cannot import from undefined module

;;         extras

;;         Expansion history:

;;         <syntax>          (##core#begin (require-extension scheme extras))
;;         <syntax>          (require-extension scheme extras)     <--
